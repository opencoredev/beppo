{
  "name": "@t3tools/contracts",
  "version": "0.0.18",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./src/index.ts",
  "exports": {
    "./settings": {
      "types": "./src/settings.ts",
      "import": "./src/settings.ts",
      "require": "./src/settings.ts"
    },
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "dev": "bunx --bun tsdown src/index.ts --format esm,cjs --dts --watch --clean",
    "build": "bunx --bun tsdown src/index.ts --format esm,cjs --dts --clean",
    "prepare": "node ../../scripts/effect-prepare.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bunx --bun vitest run"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/language-service": "catalog:",
    "@effect/vitest": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
