{
  "name": "@t3tools/desktop",
  "version": "0.0.18",
  "private": true,
  "scripts": {
    "dev": "bunx --bun electrobun dev --watch",
    "build": "bunx --bun electrobun build",
    "start": "bunx --bun electrobun run",
    "package": "bunx --bun electrobun build --env=stable",
    "typecheck": "tsc --noEmit",
    "test": "bunx --bun vitest run --passWithNoTests",
    "smoke-test": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "effect": "catalog:",
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@t3tools/contracts": "workspace:*",
    "@t3tools/shared": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "productName": "Beppo"
}
